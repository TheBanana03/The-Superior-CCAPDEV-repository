<!--CSS-->
<link rel="stylesheet" href="static/css/styles.css">

<!--JS-->
<script src="static/js/scripts.js"></script>

<!--Bootstrap-->
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>

<!--HTML-->
<!--NAVBAR-->
<nav id="navbar" class="navbar sticky-top">

    <div class="animo">
        <a href="/">A</a>
    </div>

    <div class="nav-user">
        {{#if user}}
            <div class="dropdown">
                <button class="dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    {{#if user.username}}
                        <img src="/static/assets/default.jpg" id="nav-user-icon">
                    {{else}}
                        <img src="{{user.image}}" id="nav-user-icon">
                    {{/if}}
                </button>
                <ul class="dropdown-menu">
                    <li><button id="nav-dashboard" class="dropdown-item" type="button">Dashboard</button></li>
                    <li><a href="/editprofile"><button id="nav-editprofile" class="dropdown-item" type="button">Edit Profile</button></a></li>
                    <li><a href="/logout"><button id="nav-logout" class="dropdown-item" type="button">Log Out</button></a></li>
                </ul>
            </div>
        {{else}}
            <a href="login"><button id="btn-login" type="button" class="btn btn-light">Log In</button></a>
        {{/if}}
    </div>
</nav>

<!--HEADER-->
<div class="header-homepage">
    <div class="title-homepage">
        <span>Animo</span>
    </div>
    <div>

        <!--SEARCH BAR-->
        <form class="search-bar">
            <img class="search-icon" src="static/assets/icons8-search-100.png" alt="Search Icon">
            <input type="text" placeholder="I am looking for...">
        </form>
    </div>
</div>

<!--COMMUNITIES-->
<div class="communities">
    <div class="subtitle">
        <h5>Let's get you started!</h5>
        <span>Here are some communities we think you might find relevant</span>
    </div>

    <!--COMMUNITY CARDS-->
    <div class="community-card-list row row-cols-3 row-cols-md-3 g-4">

        {{#each communities}}
        <div class="col-md-4">
            <a id="" class="community-card card h-100" href="community-page.html">
            <img src="{{image}}" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">{{name}}</h5>
                <p class="card-text">{{description}}</p>
                <div class="community-badge-container mt-auto">
                {{#each tags}}
                    <span class="badge bg-{{color}}">{{name}}</span>
                {{/each}}
                </div>
            </div>
            </a>
        </div>
        {{/each}}

        <!-- SAMPLE CARD:

            <div class="col-md-4">
                <a id="" class="community-card card h-100" href="community-page.html">
                    <img src="static/assets/cards/1.avif" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">DLSU CCS Community</h5>
                        <p class="card-text">A Hub for all DLSU College of Computer Studies Students!</p>
                        <div class="community-badge-container mt-auto">
                            <span class="badge bg-orange">🔥 Hot</span>
                            <span class="badge bg-yellow">☀ New</span>
                        </div>
                    </div>
                </a>
            </div>

        -->

    </div>
</div>